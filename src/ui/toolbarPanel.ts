/** Stable toolbar panel id — used with unregisterComponent before each addToolbarPanel. */
export const TOOLBAR_PANEL_ID = 'induced-demand-map-mode';

export const TOOLBAR_PLACEMENT = 'top-bar' as const;
